[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "btc-nq-coherence-research"
version = "0.1.0"
description = "Protocol-first research on conditional BTC–NQ co-movement and magnitude asymmetry"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pandas-market-calendars>=5.4,<6",
  "PyYAML>=6.0.2,<7",
  "scikit-learn>=1.6,<2",
  "tzdata>=2025.2,<2027",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.15,<2",
  "pytest>=8.3,<9",
  "ruff>=0.11,<1",
  "types-PyYAML>=6.0.12,<7",
]
report = [
  "matplotlib>=3.10,<4",
  "pypdf>=6.0,<7",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
btc_nq_coherence = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 96
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"report/build_report.py" = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["btc_nq_coherence"]
